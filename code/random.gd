extends Node2D
@onready var area = $Area2D

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	
#func _interact(playerArea, name, message):
	#if area.overlaps_area(playerArea):
		#if Input.is_action_just_pressed("interact"):
			#await 
